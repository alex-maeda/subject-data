# swaggerdocs (generated)

These files are generated by [swaggo/swag](https://github.com/swaggo/swag) from the handler annotations in `internal/api/`. They are committed because the `main.go` blank import (`_ "github.com/sovraai/subject-data/internal/swaggerdocs"`) must resolve at build time, including in CI where `swag` is not installed.

To regenerate after changing handler annotations:

```bash
make swagger
```

TODO: Generate these at build time instead of committing them, once the CI workflow supports a pre-build code generation step.
